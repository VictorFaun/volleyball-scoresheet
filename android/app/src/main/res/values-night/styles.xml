<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Igual que values/styles.xml, solo que acá windowBackground es el
         #121212 de modo oscuro en vez de blanco. Android elige este archivo
         en vez del otro automáticamente cuando el sistema está en modo
         oscuro (calificador "night"), gracias a que AppTheme.NoActionBar
         hereda de Theme.AppCompat.DayNight.NoActionBar. -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowBackground">#121212</item>
    </style>
</resources>
